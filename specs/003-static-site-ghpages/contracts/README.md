# Contracts

This feature is a static site only and does not expose runtime API contracts.

If future work adds server endpoints or an API surface, place OpenAPI/GraphQL contracts here (e.g., `openapi.yaml` or `schema.graphql`). For the current scope, this directory is intentionally minimal.
