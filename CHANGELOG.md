# Changelog

All notable changes to this project are documented in this file.

## [Unreleased]

- 2025-12-10: Fix: resolve `Failed to compile` caused by unsupported `next/font` imports (`Geist` / `Geist Mono`) — replaced with supported fonts; added CI smoke build and local smoke script to prevent regressions. ([#2])
