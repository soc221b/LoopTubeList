/**
 * Perf harness placeholder
 * - Measures simple resume-latency by loading app and measuring iframe load time
 * - Emits a JSON report to stdout (placeholder)
 */

(async function run() {
  console.log(JSON.stringify({ startedAt: new Date().toISOString(), note: 'placeholder harness — implement browser automation here' }))
})()
