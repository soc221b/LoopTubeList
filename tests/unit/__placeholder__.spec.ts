import { describe, it, expect } from 'vitest'

describe('placeholder test (T001a)', () => {
  it('fails intentionally until implementation (test-first)', () => {
    // This test is intentionally failing to satisfy the Test-First/TDD gate.
    expect(false).toBe(true)
  })
})
